title: TAI
description: Teaching and learning materials
remote_theme: just-the-docs/just-the-docs
plugins:
  - jekyll-include-cache

# Project site settings (critical for CSS/JS paths)
url: "https://TeachingWithAI.github.io"   # ← replace USERNAME
baseurl: "/TAI"                     # ← repo name, leading slash required

# Theme options
color_scheme: dark
search_enabled: true
